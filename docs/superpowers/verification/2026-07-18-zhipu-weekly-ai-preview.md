# 智谱周报 AI 只读预览验证记录

## 本地静态验证

- `git diff --check`：退出码 `0`，无输出。
- `npm test`：最终退出码 `0`，`269/269` 通过，失败 `0`。
- 差异凭据扫描：退出码 `0`；仅命中 `.env.example` 中两处说明性占位文本，未发现真实凭据。

## CLI 依赖与写入边界

- `weekly:ai-preview` 的入口仅接线配置读取、Base 查询、周报模板语义目标发现、AI provider 和本地预览 JSON 输出。
- 未导入消息组件或周报推送模块。
- 对预览入口、CLI 编排和预览实现的静态调用扫描未发现消息发送、工作表复制、工作表写入或 Base 写入方法调用。
- 本地 `--output` 仅创建新的 JSON 预览文件；该文件不是飞书资源写入。

## 真实只读试运行

- 执行日期：`2026-07-18`。
- 日期范围：`2026-07-13` 至 `2026-07-17`。
- 命令入口：`npm run weekly:ai-preview -- --start 2026-07-13 --end 2026-07-17 --output <本地新文件>`。
- 退出码：`0`。
- 模式：`read_only_preview`。
- Provider：`openai-compatible`。
- Model：`glm-4-flash-250414`。
- 逻辑组数量：`1`；模板业务分桶数量：`10`。
- 输出目标单元格数量：`47`；非空单元格数量：`3`，其中 `1` 个为报告周期、`2` 个为 AI 候选内容。
- evidence 单元格数量：`2`；唯一 evidence 数量：`1`。
- 首次真实输出 warnings 数量：`37`。多数为模型对空白目标返回空占位后触发；随后已修正为“明确空白不告警”，相关本地测试通过。

首次真实调用发现 `reportCount=185` 会把同一事实跨多个业务分桶重复累计。修复后使用相同飞书事实数据和本地空 Provider 重新只读计算，唯一有效事实数量为 `85`；该修复已由单元测试和完整测试覆盖。

修复后的第二次智谱调用被执行环境的数据出域策略拒绝，因此没有绕过策略重试。最终代码的统计、空白告警、事项级证据和多组隔离以 `269/269` 本地测试和相同飞书数据的本地 Provider 只读计算验证。

## 飞书无写入核对

真实预览前后只读查询结果一致：

| 资源 | 执行前 | 执行后 |
| --- | ---: | ---: |
| 周报工作表数量 | 9 | 9 |
| 周报实例表记录数 | 7 | 7 |

CLI 未创建 `LarkMessenger`，没有群消息发送能力；真实执行日志中未出现工作表复制、工作表写入、Base 写入、图片上传或消息发送操作。

## 安全边界

本文不记录日报正文、人员或资源标识符、访问 token、API key、应用密钥或完整响应内容。
