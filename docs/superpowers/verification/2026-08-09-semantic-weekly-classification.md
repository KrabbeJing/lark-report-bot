# 周报语义分类验证手册

## 验证目标

本验证只评估“单条日报事项应该归入哪个周报板块”，不评价日报写得好不好，也不直接评价最终周报文案质量。

部门周报链路应满足：

1. 周报来源映射表限定成员允许进入的模块二、模块三板块；
2. 关键词只提供提示，不作为准入门槛；
3. AI 只能在允许板块内选择一个最合适的板块；
4. 高、中置信度事项进入后续总结，低置信度事项保留给负责人判断；
5. 周五、周日任务只保存草稿快照，不直接写周报业务 Cell。

评测工具不会读写飞书。真实样本和结果必须保存在已被 Git 忽略的 `out/` 目录，禁止提交仓库。

## 合格样本标准

一条合格样本应满足：

- **单一事项：** 一行只放一条工作事项，不能把某人整天的 8 条日报合成一条。
- **保留原意：** 尽量保留日报原文，不为了方便 AI 判断而补关键词、改写结果或删减业务对象。
- **来源有效：** 来自日报统一事实表中“事实记录状态=有效”的工作总结，不使用明日/下周计划。
- **边界明确：** 根据该成员的周报来源映射，能够列出其允许进入的板块。
- **人工可判定：** 熟悉业务的人能够从允许板块中确定一个最合适的板块。
- **避免重复：** 同一事项的表单版、群聊版或复制文本只保留一条。

真正无法判断、多个板块同样合理或信息严重不足的事项，不要勉强指定答案。先放入“待讨论样本”，由两名业务人员达成一致后再进入正式评测集。

注意：当前评测的是“归入哪个板块”，不是“是否值得写进周报”。普通推进、沟通、工单等事项仍可用于分类测试；后续总结模型可以决定是否提炼或省略。

## 建议的 50 条构成

| 类型 | 建议数量 | 示例特征 |
| --- | ---: | --- |
| 明确业务词 | 8 | 直接出现“收单、银企直联、云缴费”等 |
| 缩写或内部叫法 | 7 | D0、FTP、EAST、URM+、某平台简称 |
| 无关键词但语义明确 | 10 | “完成生产参数生成并交付验证” |
| 日常运营或工单 | 7 | 商户进件、客户工单、数据核对、报表报送 |
| 沟通、讨论、协调 | 8 | 有明确业务对象，但未直接写阶段成果 |
| 同一成员可进入多个板块 | 10 | 模块二/三均允许，必须结合事项语义选择一个 |

应同时覆盖模块二和模块三，并尽量覆盖所有已启用板块。不要只挑最容易判断、关键词最明显的事项。

## 你需要填写什么

你可以直接编辑 `out/weekly-classification-labels-template.csv`，每行填写：

- `样本编号`：例如 `case-001`，不能重复；
- `日报日期`：`YYYY-MM-DD`；
- `日报事项原文`：一条原子事项；
- `日报提交人`：用于匹配来源映射，最终不会写入评测输出；
- `人工正确板块`：从该成员允许板块中选择一个；
- `人工判断备注`：简短说明为什么属于该板块，可空；
- `复核状态`：填写`已确认`后才进入正式评测。

你不需要手写 `allowedTargets`、`targetId`、业务范围、正反例等 JSON。后续由程序根据周报来源映射表、周报板块规则表生成正式的 `out/weekly-classification-labels.json`。

## 标注示例

```text
case-001,2026-07-28,完成银企直联证书更新并通过验证,张三,对公客群经营及场景建设,属于银企直联业务,已确认
case-002,2026-07-29,完成生产参数生成并交付验证,李四,收单项目组,提交人为收单来源且事项语义对应收单生产接入,已确认
```

错误示例：

- 把“沟通项目进展”改成“沟通收单项目进展”以便 AI 命中；
- 人工正确板块不在该成员的来源映射范围内；
- 同一事项以表单、群聊两个版本重复出现；
- 一条样本包含整天多项工作；
- 因为事项“不够重要”而随意归到模块三。

## 生成评测 JSON

CSV 完成人工复核后，再由程序补齐允许目标和规则信息。最终 JSON 每条记录包含：

- `evidenceId`、`date`、`text`；
- 来源映射限定的 `allowedTargets`；
- 人工答案 `expectedTargetId`。

其中 `expectedTargetId` 必须是 `allowedTargets` 中的一项，否则样本无效。

```bash
GROUPS_CONFIG_PATH=config/groups.formal.json \
npm run weekly:classification-labels -- \
  --input out/weekly-classification-labels-template.csv \
  --output out/weekly-classification-labels.json
```

该命令只读团队通讯录、周报来源映射表和周报板块规则表，不写飞书。它只接收`复核状态=已确认`的记录，并从评测 JSON 中移除姓名和 OpenID。

## 运行质量门禁

调用外部 AI 前必须获得明确批准。先在本地忽略的 `.env` 中配置 `AI_PROVIDER`、`AI_BASE_URL`、`AI_API_KEY` 和 `AI_MODEL`。使用支持思考模式的智谱模型时，可设置 `AI_CLASSIFICATION_THINKING=disabled`；该设置只影响语义分类，不改变周报总结调用。然后执行：

千问 `qwen3.8-max` 的长提示词响应可能超过默认 30 秒。评测和正式分类建议设置 `AI_TIMEOUT_MS=180000`，并且不要沿用智谱的 `AI_CLASSIFICATION_THINKING` 配置。

```bash
npm run weekly:classification-eval -- \
  --input out/weekly-classification-labels.json \
  --output out/weekly-classification-evaluation.json
```

输出文件不能预先存在。通过标准为：

- 分类准确率不低于 90%；
- 所有标注样本都必须返回一个合法分类，模型调用失败或结果缺失均不能通过；
- 模型不得返回未知样本、无效置信度等额外诊断；
- 越权目标为 0；
- 重复或多目标分类为 0；
- 所有低置信度事项都进入待负责人判断清单。

若未通过，优先优化板块的`业务范围说明`、分类正反例或提示词，不增加在线自学习，也不恢复关键词硬门槛。每次调整后使用同一份冻结样本重新评测。

## 只读周报预览

质量门禁通过后，再运行一次经批准的只读 AI Preview，人工检查：

- 没有编造数字、日期、状态、责任人或姓名；
- 不生成下周工作计划；
- 模块二能合并重复日报并提炼阶段性成果；
- 模块三不超过三条；
- 无依据时保持空白。

验证期间不启用周五、周日调度，不发送消息、不部署、不写周报 Sheet。

## 当前验证记录

- 人工样本：51 条均已复核，覆盖收单项目组、线上营业厅项目组和对公客群经营及场景建设；
- CSV 转换：已通过正式 Base 只读生成匿名评测 JSON；
- 质量门禁已增加完整性检查：任一分类失败时，即使已返回结果准确率超过 90%，也不能通过；
- `glm-4-flash-250414` 基线：51 条全部返回，正确 44 条，准确率 86.27%，未通过；
- `glm-4.7-flash`（关闭思考模式）最佳完整度运行：返回 46/51 条，其中正确 42 条，已返回准确率 91.30%；因 5 条失败，完整性门禁未通过；
- 后续复测确认智谱接口返回 HTTP 429，且 30 秒冷却后仍可能受限。本轮不再重复消耗免费额度；
- `qwen3.8-max` 使用 180 秒超时完整返回 51/51 条，正确 48 条，准确率 94.12%，无越权、重复、多目标或结构诊断，质量门禁通过；
- 千问的 6 条低置信度结果均进入负责人复核，其中 `case-003`、`case-005` 虽与人工答案不同，但没有被静默采纳；
- `case-048`“笔笔扣业务服务号运营方案”被中置信度错分至模块三。本地对照测试证明，在收单项目组“包含主题”增加“笔笔扣”并补充同类正例后，可高置信度归入收单；
- 飞书写入、消息发送和定时调度：本轮均未启用。

## 本轮规则数据发现

正式“周报板块规则表”中，线上营业厅项目组和收单项目组原有的错位样例已完成修正，并已重新生成评测 JSON。当前还需补充一项业务别名：

- 收单项目组“包含主题”增加“笔笔扣”；
- 收单项目组“分类正例”可增加“与分行及合作方讨论笔笔扣业务服务号运营方案”；
- 正反例描述板块语义边界，不用于判断事项是否足够重要、是否已经形成阶段性成果。
